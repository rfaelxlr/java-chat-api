CREATE TABLE guild_users (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   user_id BIGINT,
   guild_id BIGINT,
   CONSTRAINT pk_guild_users PRIMARY KEY (id)
);

ALTER TABLE guild_users ADD CONSTRAINT FK_GUILD_USERS_ON_GUILD FOREIGN KEY (guild_id) REFERENCES guilds (id);

ALTER TABLE guild_users ADD CONSTRAINT FK_GUILD_USERS_ON_USER FOREIGN KEY (user_id) REFERENCES users (id);
